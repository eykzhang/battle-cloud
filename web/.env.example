# The API this client talks to. Defaults to the deployed service, so `npm run dev`
# works with no configuration; point it at http://localhost:8080 to develop against
# compose.
VITE_API_BASE_URL=https://m6tky2d13e.execute-api.us-east-2.amazonaws.com
